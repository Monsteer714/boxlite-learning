## [2026-08-12] ingest | BoxLite Reference Documentation

- Ingested the BoxLite Reference Documentation (README-20260812-1786500247075.md): canonical API/configuration/error-handling reference.
- Added entities: CodeBox, BrowserBox, ComputerBox, InteractiveBox, BoxOptions.
- Added concepts: host.boxlite.internal, Capability Overrides (advanced.capabilities), BoxLite Error Taxonomy, SQLite Metadata Persistence.
- Notable findings: `allow_net` hostname-only rules deny all UDP egress (TLS SNI/HTTP Host enforcement); `host.boxlite.internal` exposes host loopback services under empty `allow_net`; secret placeholder protocol (`<BOXLITE_SECRET:{name}>`, `BOXLITE_SECRET_<NAME>`); OCI `EXPOSE` is metadata-only; port forwarding is TCP-only; `env` values visible in box metadata (footgun vs `secrets`); `auto_remove` defaults to True; 12-variant error taxonomy documented; Podman-style SQLite persistence.
