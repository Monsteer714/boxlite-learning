---
type: source
title: "Source: README-20260812-1786500247054.md"
created: 2026-08-12
updated: 2026-08-12
sources: ["README-20260812-1786500247054.md"]
tags: []
related: []
---

# Source: README-20260812-1786500247054.md

# Source Analysis: C SDK API Reference

**Language:** English (matches source)

---

## Key Entities

| Entity | Type | Role | Already in Wiki? |
|---|---|---|---|
| `libboxlite` | C library (FFI bindings) | Central — the entire document is its API reference | No — new entity (could be a page on the C SDK) |
| `boxlite.h` | Header file | Central — public C interface | No |
| `CBoxliteSimple` (`boxlite_simple_*`) | Simple API handle type | Central — convenience layer; C binding of the SimpleBox concept | Partially — [[entities/simplebox\|SimpleBox]] covers the high-level API but not C-specific names |
| `CBoxliteRuntime` | Runtime handle | Central — maps to BoxLite Runtime | Partially — [[entities/boxlite-runtime\|BoxLite Runtime]] exists but lacks C FFI details |
| `CBoxHandle` | Box handle | Central — maps to [[entities/box\|Box]] | Partially |
| `CBoxliteOptions` / `CAdvancedBoxOptions` | Typed configuration structs | Central — new API surface detail (capabilities add/drop, network enable) | No detail in wiki |
| `BoxliteErrorCode` | Enum (20 error codes) | Central — structured error handling model | No |
| `CBoxliteError` | Error struct (code + message) | Central | No |
| `CExecutionHandle` | Execution handle (streaming, write/wait/kill/resize) | Central — maps to LiteBox exec capabilities | Partially — [[entities/litebox\|LiteBox]] covers runtime handle |
| `CBoxNetworkHandle` / `CBoxTunnelHandle` | Network tunnel handles | Central — one-shot connection semantics, distinct from port listeners | No |
| `CBoxInfo` / `CBoxInfoList` / `CNetworkInfo` / `CPublishedPortList` / `CPublishedPort` | Introspection data types | Central — async callback-based discovery | No |
| `CRuntimeMetrics` / `CBoxMetrics` | Metrics structs | Peripheral | No |
| `BoxliteImageRegistry` / `BoxliteRegistryTransport` | Registry configuration | Peripheral — custom registries, TLS skip-verify, auth | No |
| `boxlite_runtime_drain()` | Async dispatch function | Central — post-and-drain callback pattern | No |
| `boxlite_box_id`, `boxlite_get` | Reattachment functions | Central — cross-process box reattachment | Related to [[concepts/detached-boxes\|Detached Boxes]] but not explicitly |

---

## Key Concepts

| Concept | Definition | Why It Matters | In Wiki? |
|---|---|---|---|
| **Two API Styles** | Simple (`boxlite_simple_*` — auto-managed runtime, buffered results) vs Native (`boxlite_*` — full config, streaming, advanced features) | Core design decision; mirrors SimpleBox vs runtime-level distinction | Implicit in [[entities/simplebox\|SimpleBox]] vs [[entities/litebox\|LiteBox]], but not documented as a C SDK pattern |
| **Structured Error Handling** | All functions return `BoxliteErrorCode` (20-value enum) + out-param `CBoxliteError` with message | Replaces v0.1.x `char* error` pattern; enables switch-based programmatic handling and retry logic | No |
| **Ownership Rules** | Caller frees strings via `boxlite_free_string()`, errors via `boxlite_error_free()`, results vi
