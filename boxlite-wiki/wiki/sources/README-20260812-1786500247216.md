---
type: source
title: "Source: README-20260812-1786500247216.md"
created: 2026-08-12
updated: 2026-08-12
sources: ["README-20260812-1786500247216.md"]
tags: []
related: []
---

# Source: README-20260812-1786500247216.md

# Source Analysis: BoxLite Runner README

## Key Entities

| Entity | Type | Role | In Wiki Index? |
|---|---|---|---|
| **BoxLite Runner** | Go HTTP server (`apps/runner`) | **Central** — the entire document's subject; production multi-tenant REST + WebSocket front to BoxLite | ❌ **Missing** (wiki has `boxlite-cli`, `boxlite-runtime`, `boxlite-control-plane`, but no runner entity) |
| **`boxlite.Client`** | FFI wrapper (Go SDK ↔ Rust) | Central — single shared handle to the Rust runtime; all services share one instance | Partially — related to `boxlite-runtime` |
| **`BoxliteRuntime`** | Rust SDK core (linked in-process via cgo) | Central — the embedded runtime the runner fronts | ✅ `boxlite-runtime` (but described as embedded library, not via Go FFI) |
| **`ExecManager`** (`pkg/boxlite/exec_manager.go`) | Go service; registry of `ManagedExec` instances | Central — owns execution lifecycle, cleanup/reaping loop | ❌ Missing |
| **`ManagedExec`** | Per-execution state object | Central — attach slot, reconnect flags, reap timers | ❌ Missing |
| **`BoxSyncService`** | Background goroutine | Important — 10 s local→remote state reconciliation | ❌ Missing |
| **`metrics.Collector`** | Background goroutine | Important — ring-buffered CPU/allocation snapshots | ❌ Missing |
| **`BackupInfoCache`** / **`ArtifactErrorCache`** | In-process TTL caches | Important — carry async operation failures across request boundaries | ❌ Missing |
| **v2 poller / executor / healthcheck** (`pkg/runner/v2/`) | Background services (when `BOXLITE_API_VERSION=2`) | Important — production path: pull-based job execution from control plane | ❌ Missing |
| **NestJS API service** | Control-plane HTTP service | Peripheral-to-central — issues jobs, collects healthchecks | Related to `boxlite-control-plane` |
| **xterm.js toolbox proxy** | Legacy terminal preview route | Peripheral — dashboard-only convenience surface | ❌ Missing |
| **libkrun / guest agent / vsock** | Lower stack | Peripheral (diagram context) | ✅ Present |
| **SST EC2 / `sst.config.ts`** | Infra | Peripheral — production deployment bootstrap | Related to `boxlite-control-plane` |
| **Go SDK (`sdks/go/`)** | SDK | Central dependency — FFI bridge to Rust | ⚠️ Implicit via `boxlite-runtime`; not its own entity |

## Key Concepts

| Concept | Definition | Why it matters | In Wiki? |
|---|---|---|---|
| **Runner process model** | One process = one `boxlite.Client`; HTTP server + background goroutines share it; SIGTERM drains 5 s | Central architecture claim — concurrency safety through a single FFI handle | ❌ Missing |
| **Single-attach semantics** | Each execution attaches to at most one WS client; second attach → HTTP 409 *before* upgrade | Preserves PTY semantics; prevents stdin/stdout races | ❌ Missing |
| **Detach-without-kill** | Connection drop doesn't kill process; reattach within grace reuses same execution_id | Survives LB idle timeouts / network blips; matches E2B model | Related to `detached-boxes` (box-level
