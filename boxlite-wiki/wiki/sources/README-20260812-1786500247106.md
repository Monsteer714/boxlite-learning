---
type: source
title: "Source: README-20260812-1786500247106.md"
created: 2026-08-12
updated: 2026-08-12
sources: ["README-20260812-1786500247106.md"]
tags: []
related: []
---

# Source: README-20260812-1786500247106.md

# Source Analysis: BoxLite README — How-to Guides (2026-08-12)

**Language note:** The source is predominantly English, so this analysis is in English. One notable exception: the first lifecycle link title is in Chinese (「AutoStop、AutoResume 与 AutoDelete」), which may matter for the wiki's language consistency.

---

## Key Entities

| Entity | Type | Role | In wiki? |
|---|---|---|---|
| **BoxLite** | Product/runtime | Central — the subject of all guides | Yes — `[[entities/boxlite]]` |
| **SimpleBox** | High-level SDK API | Central — foundation pattern in Example 1, used throughout | Yes — `[[entities/simplebox]]` |
| **CodeBox** | High-level SDK API (new) | Central — secure Python code execution for AI agents; used in AI agent, FastAPI, Celery, serverless examples | **No — should be created.** Sibling of SimpleBox; the document treats it as a first-class abstraction |
| **BrowserBox** | High-level SDK API (new) | Peripheral — browser automation via Playwright (web scraping, E2E, screenshots) | **No — new** |
| **ComputerBox** | High-level SDK API (new) | Peripheral — desktop automation (screenshot, clicks, typing, mouse movement; "9 more functions") | **No — new** |
| **Box / BoxOptions** | Core entity + options struct | Central — `BoxOptions` used everywhere (image, cpus, memory_mib, disk_size_gb, ports, volumes, env, working_dir) | Yes — `[[entities/box]]`, `[[entities/simplebox]]` |
| **BoxLite Runtime** | Runtime library | Central — `runtime.create()`, `runtime.get()`, `runtime.list_info()`, `runtime.metrics()` | Yes — `[[entities/boxlite-runtime]]` |
| **gvproxy** | Network backend | Central — default NAT networking; troubleshooting section references it directly | Yes — `[[entities/gvproxy]]` |
| **KVM / Hypervisor.framework** | Hypervisors | Peripheral — platform matrix; deployment prerequisites | Partially — `[[entities/libkrun]]` covers the VMM layer |
| **Playwright** | External tool | Peripheral — used inside BrowserBox | No |
| **FastAPI, Celery, AWS Lambda** | External integration targets | Peripheral — integration examples only | No (out of scope probably) |
| **Docker / Kubernetes** | Deployment platforms | Peripheral — deployment patterns require privileged KVM passthrough | No |

**Gap analysis:** This document introduces three new high-level API surfaces (CodeBox, BrowserBox, ComputerBox) not in the wiki index. The existing wiki has `SimpleBox` and `LiteBox` but no umbrella page for the high-level SDK family.

---

## Key Concepts

| Concept | Definition | Matters because | In wiki? |
|---|---|---|---|
| **Port publication** | Explicit `(host_port, guest_port, proto)` TCP mapping; local runtime owns the host listener for the box's lifetime; accepts repeated connections | The only way normal host apps (browsers, DB clients) reach guest services; `EXPOSE` is metadata-only, never creates listeners | **No — new.** Closely related to gvproxy port publishing but documented here at SDK level |
| **Network tunnel** | `box.network.tunn
