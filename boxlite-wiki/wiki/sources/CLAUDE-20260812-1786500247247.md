---
type: source
title: "Source: CLAUDE-20260812-1786500247247.md"
created: 2026-08-12
updated: 2026-08-12
sources: ["CLAUDE-20260812-1786500247247.md"]
tags: []
related: []
---

# Source: CLAUDE-20260812-1786500247247.md

# Source Analysis: CLAUDE.md — apps/dashboard

**Document type:** Working-context file (CLAUDE.md) for AI-assisted development on the BoxLite dashboard frontend. It is prescriptive and descriptive — it defines the current engineering goal ("Face-Swap" UI rewrite), the architectural seam that makes it safe, and hard constraints that must survive.

---

## Key Entities

| Entity | Type | Role | In Wiki? |
|---|---|---|---|
| **apps/dashboard** | Codebase/frontend app | **Central** — the entire document is its operating manual | ❌ No (new domain) |
| **BoxLite compute platform** | Product/platform | Central (background) — the platform the dashboard consoles | ✅ `[[entities/boxlite\|BoxLite]]` |
| **@boxlite-ai/api-client** | Generated OpenAPI client package | Central — sole gateway to server APIs | ❌ No |
| **@boxlite-ai/analytics-api-client** | Generated OpenAPI client package | Peripheral — only for analytics/usage | ❌ No |
| **`src/api/apiClient.ts`** | API wrapper module | Central — wraps generated clients, injects OIDC token | ❌ No |
| **React Query hooks** (`queries/*` ×19, `mutations/*` ×27) | Data layer | **Central** — the "functional contract" the UI must preserve | ❌ No |
| **OIDC / `react-oidc-context`** | Auth mechanism | Peripheral — auth for the console | ❌ No |
| **socket.io + Svix** | Realtime transport + webhook delivery | Peripheral — box/runner/volume events, webhook portal | ❌ No |
| **MSW (Mock Service Worker)** | Mock tool | Central to workflow — backend-free dev via `npm run start:mock` | ❌ No |
| **PostHog** | Product analytics | Peripheral — part of the load-bearing provider chain | ❌ No |
| **Billing page** (PlanSection, WalletSection, UsageSection, BillingAlerts) | Feature/components | Peripheral — one page, three un-routed sections | ❌ No |
| **Boxes page** (list + detail + terminal + lifecycle) | Feature | Central — "the core," ~80% of visible value | ❌ Partially (`Box` runtime entity exists, not the UI) |
| **PR #820** (`feat/dashboard-floating-restyle`) | Engineering artifact | Peripheral — the current rewrite effort | ❌ No |
| **Vite, React, TypeScript, Tailwind, shadcn/ui (Radix), nx, yarn** | Stack | Peripheral — context only | ❌ No |

---

## Key Concepts

| Concept | Definition | Why it matters | In Wiki? |
|---|---|---|---|
| **Face-Swap UI Rewrite** | Rebuild the entire presentation layer with **zero new server APIs**; server contract frozen | Defines the current goal and the risk model (safe only because of the seam) | ❌ No |
| **Functional contract seam** | UI never touches HTTP; flow is `pages/components → hooks → ApiClient → generated clients` | The clean boundary that makes the rewrite tractable; the document's central architectural claim | ❌ No (distinct from runtime's `rest-api-websocket-exec`) |
| **Preserve-vs-Rebuild rule** | "If a file decides **what data to fetch / how state flows / who you are**, preserve it. If it decides **how it looks**, rebuild it." | The operational rule of thumb for the rewrit
